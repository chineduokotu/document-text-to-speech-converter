services:
  - type: web
    name: text-to-speech-app
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python run_webapp.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
      - key: FLASK_ENV
        value: production